services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      DB_HOST: ${DB_HOST:-100.116.169.44}
      DB_PORT: ${DB_PORT:-49154}
      DB_USER: ${DB_USER:-n8n}
      DB_PASS: ${DB_PASS:-n8n}
      DB_NAME: ${DB_NAME:-bft_tracker}
      PORT: 3000
